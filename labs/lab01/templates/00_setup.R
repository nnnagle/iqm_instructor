# GEOG 415 — Lab 1 setup
# Run this script first each time you begin work.

library(tidyverse)
library(sf)

# Make large numbers easier to read in the Console.
options(scipen = 999)

# We will build figures explicitly in the lab.
# No data are loaded by this setup script.
